{
  "name": "tron-protocol-typescript",
  "version": "1.0",
  "scripts": {
    "clean": "node rimraf.config.js",
    "proto": "buf mod update && buf generate",
    "build": "tsc && rollup -c"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^5.0.5",
    "globby": "^14.0.1",
    "rimraf": "^5.0.5",
    "rollup": "^4.12.0",
    "rollup-plugin-cleanup": "^3.2.1",
    "rollup-plugin-dts": "^6.1.0",
    "typescript": "^5.3.3"
  }
}
